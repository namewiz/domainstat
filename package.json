{
  "name": "fast-domain-status",
  "version": "0.1.0",
  "description": "Extremely fast domain status checker library that a combination DOH, HOST, PING, RDAP, WHOIS etc",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "npx -y esbuild src/index.ts --bundle --minify --format=esm --target=es2020 --outdir=dist/ --platform=node",
    "test": "npm run build && node ./tests/test.js",
    "start": "npm run build && cp dist/index.js demo/index.js && npx serve demo"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tldts": "^7.0.10"
  },
  "devDependencies": {
    "@types/node": "^24.1.0"
  }
}
